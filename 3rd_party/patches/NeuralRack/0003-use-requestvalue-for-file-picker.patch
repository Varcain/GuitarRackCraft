diff --git a/NeuralRack/gui/widgets.cc b/NeuralRack/gui/widgets.cc
index d1eec42..ffd19fa 100644
--- a/NeuralRack/gui/widgets.cc
+++ b/NeuralRack/gui/widgets.cc
@@ -1224,6 +1224,26 @@ static void my_fdialog_response(void *w_, void* user_data) {
 static void my_fbutton_callback(void *w_, void* user_data) {
     Widget_t *w = (Widget_t*)w_;
     FileButton *filebutton = (FileButton *)w->private_struct;
+    /* If host provides ui:requestValue (Android), use native file picker.
+       Check before HAS_POINTER — our X11 server doesn't send EnterNotify. */
+    if (adj_get_value(w->adj)) {
+        Widget_t *par = (Widget_t*)w->parent;
+        X11_UI *ui = (X11_UI*)par->parent_struct;
+        if (ui->itf.request_value) {
+            typedef struct { void* handle;
+                uint32_t (*request)(void*, uint32_t, uint32_t, const void*);
+            } RV_t;
+            RV_t *rv = (RV_t*)ui->itf.request_value;
+            LV2_URID key = 0;
+            if (w->data == -1) key = ui->itf.uris.neural_model;
+            else if (w->data == -2) key = ui->itf.uris.neural_model1;
+            else if (w->data == -3) key = ui->itf.uris.conv_ir_file;
+            else if (w->data == -4) key = ui->itf.uris.conv_ir_file1;
+            if (key) rv->request(rv->handle, key, 0, NULL);
+            adj_set_value(w->adj, 0.0);
+            return;
+        }
+    }
     if (w->flags & HAS_POINTER && adj_get_value(w->adj)){
         filebutton->is_active = true;
         if (!filebutton->w) {
@@ -1237,8 +1257,8 @@ static void my_fbutton_callback(void *w_, void* user_data) {
 #ifdef _OS_UNIX_
             Atom wmStateAbove = XInternAtom(w->app->dpy, "_NET_WM_STATE_ABOVE", 1 );
             Atom wmNetWmState = XInternAtom(w->app->dpy, "_NET_WM_STATE", 1 );
-            XChangeProperty(w->app->dpy, filebutton->w->widget, wmNetWmState, XA_ATOM, 32, 
-                PropModeReplace, (unsigned char *) &wmStateAbove, 1); 
+            XChangeProperty(w->app->dpy, filebutton->w->widget, wmNetWmState, XA_ATOM, 32,
+                PropModeReplace, (unsigned char *) &wmStateAbove, 1);
 #elif defined _WIN32
             os_set_transient_for_hint(w, filebutton->w);
 #endif
diff --git a/NeuralRack/lv2/lv2_plugin.cc b/NeuralRack/lv2/lv2_plugin.cc
index 2eaed34..779029f 100644
--- a/NeuralRack/lv2/lv2_plugin.cc
+++ b/NeuralRack/lv2/lv2_plugin.cc
@@ -265,6 +265,7 @@ static LV2UI_Handle instantiate(const LV2UI_Descriptor * descriptor,
 
     ui->parentXwindow = 0;
     ui->private_ptr = NULL;
+    ui->itf.request_value = NULL;
     ui->need_resize = 1;
     ui->loop_counter = 4;
     ui->uiKnowSampleRate = false;
@@ -291,6 +292,8 @@ static LV2UI_Handle instantiate(const LV2UI_Descriptor * descriptor,
             ui->itf.resize = (LV2UI_Resize*)features[i]->data;
         } else if (!strcmp(features[i]->URI, LV2_URID_URI "#map")) {
             ui->itf.map = (LV2_URID_Map*)features[i]->data;
+        } else if (!strcmp(features[i]->URI, LV2_UI__requestValue)) {
+            ui->itf.request_value = features[i]->data;
         }
     }
 
diff --git a/NeuralRack/lv2/lv2_plugin.h b/NeuralRack/lv2/lv2_plugin.h
index 8d9c6f9..5847953 100644
--- a/NeuralRack/lv2/lv2_plugin.h
+++ b/NeuralRack/lv2/lv2_plugin.h
@@ -38,6 +38,10 @@
 #define LV2_UI__scaleFactor  "http://lv2plug.in/ns/extensions/ui#scaleFactor"
 #endif
 
+#ifndef LV2_UI__requestValue
+#define LV2_UI__requestValue "http://lv2plug.in/ns/extensions/ui#requestValue"
+#endif
+
 
 #define PLUGIN_UI_URI "urn:brummer:neuralrack_ui"
 
@@ -88,7 +92,8 @@ struct Interface {
     LV2UI_Write_Function write_function;
     LV2UI_Resize* resize;
     LV2_Atom_Forge forge;
-    X11LV2URIs   uris;    
+    X11LV2URIs   uris;
+    void *request_value;
 };
 
 #include "widgets.h"
