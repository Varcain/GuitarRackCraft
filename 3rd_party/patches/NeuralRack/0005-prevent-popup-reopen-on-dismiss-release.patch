diff --git a/libxputty/xputty/header/xputty.h b/libxputty/xputty/header/xputty.h
index 5f9efa9..20423f9 100644
--- a/libxputty/xputty/header/xputty.h
+++ b/libxputty/xputty/header/xputty.h
@@ -263,6 +263,8 @@ struct Xputty{
     bool run;
 /** bool to indicate if button is pressed on grab window */
     bool is_grab;
+/** bool to consume the ButtonRelease after a popup dismiss */
+    bool dismiss_consume_release;
 /** float to hold the current dpi setting of the Display */
     float hdpi;
 #ifdef _OS_UNIX_
diff --git a/libxputty/xputty/xputty.c b/libxputty/xputty/xputty.c
index 2a79f11..a6fc816 100644
--- a/libxputty/xputty/xputty.c
+++ b/libxputty/xputty/xputty.c
@@ -48,6 +48,7 @@ void main_init(Xputty *main) {
     main->submenu = NULL;
     main->run = true;
     main->is_grab = false;
+    main->dismiss_consume_release = false;
 #if defined(PAWPAW)
     main->small_font = 11 * main->hdpi;
     main->normal_font = 13 * main->hdpi;
diff --git a/libxputty/xputty/xwidget-linux.c b/libxputty/xputty/xwidget-linux.c
index 0bd1a17..8ae5aee 100644
--- a/libxputty/xputty/xwidget-linux.c
+++ b/libxputty/xputty/xwidget-linux.c
@@ -265,6 +265,10 @@ void os_widget_event_loop(void *w_, void* event, Xputty *main, void* user_data)
             XButtonEvent *xbutton = &xev->xbutton;
             _check_grab(wid, xbutton, main);
             _check_submenu(wid, xbutton, main);
+            if (main->dismiss_consume_release) {
+                main->dismiss_consume_release = false;
+                break;
+            }
             if (wid->state == 4) break;
             if (xbutton->button == Button1) {
                 if ((wid->flags & IS_POPUP) == 0) {
@@ -1037,6 +1041,7 @@ void os_run_embedded(Xputty *main) {
                     XUngrabPointer(main->dpy,CurrentTime);
                     widget_hide(main->hold_grab);
                     main->hold_grab = NULL;
+                    main->dismiss_consume_release = true;
                 }
             } else if(main->hold_grab != NULL && ((main->hold_grab->flags & IS_POPUP) == 0)) {
                 main->hold_grab->func.button_press_callback(main->hold_grab, &xev.xbutton, NULL);
